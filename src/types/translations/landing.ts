export interface LandingTranslations {
  quotes: Array<{
    text: string;
    author: string;
  }>;
  buttons: {
    articles: string;
    about: string;
  };
  sections: {
    latest: string;
  };
} 