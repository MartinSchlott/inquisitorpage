export interface HeaderTranslations {
  title: string;
  subtitle: string;
  theme: {
    label: string;
    options: {
      light: string;
      dark: string;
      auto: string;
    };
  };
  language: {
    label: string;
  };
}